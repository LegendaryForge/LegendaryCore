# LegendaryCore build notes

- Hytale runtime requires Java 25.
- Gradle is intentionally run on Java 21 for toolchain stability.
- Use ./gradlew21 for builds to avoid Java 25 native-access warnings.

